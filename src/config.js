const config = {
    server: {
        host: 'http://127.0.0.1/freeClass'
    },
    code: {
        success: '0000',
        fail: '500'
    }
}

config.server.host = config.server.host;

export default config;